# A throwaway spike for #488: how much of the Kafka wire protocol does a real
# client need before it can read a Felix stream?
#
# Outside the workspace on purpose. This is evidence, not a product: it exists
# to be measured and then deleted or promoted, and it must not become something
# `task lint` keeps alive by habit.
[package]
name = "felix-kafka-shim-spike"
version = "0.0.0"
publish = false
edition = "2024"

[dependencies]
anyhow = "1"
bytes = "1"
# CRC-32C (Castagnoli), which the v2 record batch requires and `crc32fast`
# (IEEE) is not. Clients validate it, so this is not optional.
crc = "3"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "io-util", "time"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
# A real broker behind the shim. AGPL-3.0, like anything embedding the broker —
# which is itself one of the decisions, since a shim would ship inside Felix.
felix-broker = { path = "../../crates/felix-broker" }
felix-storage = { path = "../../crates/felix-storage" }

[workspace]
