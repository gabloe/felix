# `felix-client-linux-arm64-gnu`

The linux-arm64-gnu binary for [`felix-client`](https://www.npmjs.com/package/felix-client).

Install `felix-client` instead — it declares this package as an optional
dependency and npm fetches only the one matching your platform.

Documentation: https://gabloe.github.io/felix/clients/typescript/
