# felix-common

Shared types and utilities used across Felix.

Responsibilities:
- ID types for regions, tenants, namespaces, streams, topics, and shards
- Common configuration structs and defaults
- Shared error types and result aliases
