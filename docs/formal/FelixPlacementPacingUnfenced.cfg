\* The same, with each start conditional only on its own shard's generation,
\* which is how placement wrote before the token: the limits then held per
\* planner.
\*
\* Expected to violate CopiesWithinLimit: two planners read one free slot,
\* and each starts a copy on a different shard.
\* Evidence: this configuration REMOVES the token check, so the citation is
\* what proves the check is there to remove.
\* `two_instances_cannot_both_take_the_last_move_slot` -- the second of two
\*   starts decided from the same free slot is fenced.

SPECIFICATION Spec

CONSTANTS
    Shards = {s1, s2, s3}
    Nodes = {n1, n2, n3}
    MaxConcurrent = 1
    MaxPerNode = 1
    CountReplacements = TRUE
    Planners = {p1, p2}
    Operators = {p2}
    Fenced = FALSE

SYMMETRY Symm

INVARIANTS
    CopiesWithinLimit
