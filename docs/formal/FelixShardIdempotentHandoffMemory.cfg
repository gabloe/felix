\* The move of FelixShardIdempotentHandoff.cfg with the producer sequences
\* kept in the leader's memory. TLC is expected to find the new leader storing
\* the re-sent write a second time (NoDuplicate).
\* Evidence: none, this models the in-memory table the broker no longer uses
\* for a durable stream. The passing configuration cites the tests showing the
\* log is what is consulted.

SPECIFICATION Spec

CONSTANTS
    Brokers = {a, b, c}
    L = 4
    Margin = 2
    Eps = 1
    Drift = 0
    MaxTime = 5
    MaxWrites = 1
    CheckAtCommit = TRUE
    Quorum = TRUE
    Promotion = "leader-report"
    ReportBeforeAck = TRUE
    Handoff = TRUE
    WaitForDrained = TRUE
    FenceAtClaim = TRUE
    AckOnAdmit = FALSE
    FenceFromAdmit = TRUE
    MaxMoves = 1
    Planners = {}
    CasWrites = TRUE
    StageMove = FALSE
    LearnerVotes = FALSE
    Resends = TRUE
    SequencesInLog = FALSE
    Cancel = FALSE
    CancelCas = TRUE
    ReportBound = "acknowledged"

SYMMETRY Symm

INVARIANTS
    TypeOK
    AtMostOneServing
    AckedSurvive
    AckedAgree
    NoTruncationBelowHwm
    NoStaleCommit
    AckedOnMajority
    NoDuplicate
