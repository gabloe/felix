\* The same, with a follower replacement invisible to the count, which is
\* how a replacement used to be written: the new follower swapped in with
\* nothing in the assignment saying a copy was running.
\*
\* Expected to violate CopiesWithinLimit: a replacement starts, and a move
\* starts beside it because the count sees no copy in flight.
\* Evidence: this configuration REMOVES what the implementation counts, so the
\* citation is what proves the count is there to remove.
\* `a_follower_replacement_holds_a_move_slot` -- a replacement holds a move
\*   slot until the follower it copies in is seated.

SPECIFICATION Spec

CONSTANTS
    Shards = {s1, s2, s3}
    Nodes = {n1, n2, n3, n4}
    MaxConcurrent = 1
    MaxPerNode = 1
    CountReplacements = FALSE
    Planners = {p1}
    Operators = {}
    Fenced = TRUE

SYMMETRY Symm

INVARIANTS
    CopiesWithinLimit
