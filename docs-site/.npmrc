# Resolve from the public registry regardless of any user- or machine-level
# registry override. A mirror's URLs and its weaker sha1 integrity hashes end up
# baked into package-lock.json, which then points a public repo at a private feed.
registry=https://registry.npmjs.org/
